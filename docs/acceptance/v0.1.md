# v0.1 验收清单

验收人：________  日期：________

## 知识底座

- [ ] 仓库存在 `AGENTS.md`、`ROADMAP.md`、`docs/INDEX.md`
- [ ] `.cursor/rules/` 与 `.cursor/skills/` 可读
- [ ] `docs/upstream.md` 钉死 tag 为 `1.136.1`

## 产品壳

- [ ] 能启动 su（便携包或本地构建产物）
- [ ] 窗口/关于信息体现产品名 **su**（非 Code / VSCodium）
- [ ] 能打开本地文件夹并编辑文件

## su-ai 空壳

- [ ] 命令面板可找到 `Su: Open Settings`（或等价命令）
- [ ] 设置中可见 `su.baseUrl` / `su.apiKey` / `su.model` / `su.timeoutMs` 占位项

## 打包 / 开发态

- [ ] 存在 Windows x64 产物（zip 或安装包），路径：________
- [ ] **或** 使用 `.\scripts\run-dev.ps1` 启动开发壳完成上述产品壳与 su-ai 验收（全量包构建中时可先此项）
- [ ] 若本机 `npm ci` 报 `MSB8040`：已安装 Spectre 库并重试，**或**已用 GitHub Actions `build-win` 出包

## 备注

_（体验问题写这里）_

### 已知阻塞（2026-09-17）

- 本机曾缺 Spectre → 已可安装通过
- 本机 `npm ci` 可能因 **GitHub API rate limit 403** 无法下载 `@vscode/ripgrep` 预编译包
- **推荐**：用 GitHub Actions `build-win` 出包（步骤见 `docs/build-windows.md`）
- CI 曾因误用 `vscode-win32-x64-min-ci`（缺 `out-vscode-min`）失败 → 已改为 `bundle-vscode` + `vscode-win32-x64-ci`
- CI 曾因打包路径误跑 `compile-client`（`tsgo exited with code 2`）失败 → 已从 `build-win.ps1` 移除；打包不依赖 `out/`
- CI 曾因 `compile-build-without-mangling` 内 tsgo 原生崩溃（exit 2、0 errors）失败 → patch 0002 容忍该场景（真实类型错误仍挡构建）
- OSS 跳过 Copilot 时打包会卡在 ripgrep shim → 已用 patch `0001-skip-copilot-shim`
