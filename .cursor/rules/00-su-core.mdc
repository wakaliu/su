---
description: su 核心约束 — 先查索引、禁止幻觉改 vendor、版本边界
alwaysApply: true
---

# su 核心规则

1. 改代码前先读 `ROADMAP.md` 与 `docs/INDEX.md`，再读相关 `docs/modules/*.INDEX.md`。
2. 加载对应 `.cursor/skills/*/SKILL.md`；不要把整个 `vendor/vscode` 无目的塞进上下文。
3. **禁止**直接修改 `vendor/vscode` 而不生成 `patches/` 条目；无登记补丁视为缺陷。
4. 不跨当前 ROADMAP 版本实现功能（例如 v0.1 不做完整 Agent）。
5. 不提交密钥；不编造不存在的文件路径或上游 API。
6. 功能变更须同步模块 INDEX 与 `docs/acceptance/v0.x.md`。
7. 对用户使用简体中文。
