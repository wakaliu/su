# Placeholder icon assets for v0.1.

Replace with proper Windows `.ico` / PNG set before public release branding polish.
Build scripts currently rely on upstream default icons if custom files are absent.
